// /*jshint expr: true */

// /* jshint ignore:start */
// var chai      = require('chai');
// var sitesSrvc = require('../../../server/services/sites');

// var expect = chai.expect;

// describe.skip('services', function () {
//     describe('Create Site', function () {
//         it('should have 2 fields in list', function () {
//             return expect(propertyCount()).to.eventually.equal(2);

//             function propertyCount() {
//                 return sitesSrvc.listSites({})
//                     .then(function (sites) {
//                         var length = Object.getOwnPropertyNames(sites[0]._doc).length;
//                         return length;
//                     });
//             }
//         });

//         it('should not be deleted if has clients', function () {
//             return expect(sitesSrvc.deleteSite(testSite)).to.be.rejected;
//         });
//     });
// });
// /* jshint ignore:end */
