div(ng-include="'views/dashboard/shell.html'")
h1 New {{event.eventType}}: {{event.eventSummary}}
div(ng-include="'views/dashboard/communication/navbar-communication.html'")
.container
    //- .row
        .col-xs-12
            p.text-muted Created by:&nbsp;
                span.text-default {{ event.createdByName }}
    .row.form-horizontal
        .col-sm-6
            .form-group
                label.col-sm-4.control-label Note type
                .col-sm-8
                    select.form-control(ng-model="event.eventType")
                        option(value="Admin note") Admin
                        option(value="Service note") Service
    .row.form-horizontal
        .col-sm-6
            .form-group
                label.col-sm-4.control-label(for="clientName") Client
                .col-sm-8
                    client-multi-selector(ng-model="appointment.clients" placeholder="client name(s)")
        .col-sm-6(ng-show="event.eventType==='Service note'")
            .form-group
                label.col-sm-4.control-label(for="resourceSelector") Service provider
                .col-sm-8
                    user-multi-selector(ng-model="appointment.resources" placeholder="service provider(s)")
        
    hr
    .row.form-horizontal
        .form-group
            label.col-sm-2.control-label Note
            .col-sm-10
                textarea.form-control(msd-elastic="\n" ng-model="event.body" placeholder="note")
        .form-group
            .col-sm-2.col-sm-offset-2
                .well.text-center(ngf-select ngf-drop='', ng-model='files', ngf-drag-over-class='dragover', ngf-multiple='true', ngf-allow-dir='true', ngf-accept="image/*,application/pdf") Drop attachments here, or click to browse
                    div(ngf-no-file-drop) File Drag/Drop is not supported for this browser
            .col-sm-8
                p (list of icons of uploaded files)
        .form-group
            a(href="#" ng-click="hideOtherDetails = !hideOtherDetails")
                span(ng-hide="hideOtherDetails") Edit summary...
                span(ng-show="hideOtherDetails") Hide summary...
            div(ng-show="hideOtherDetails")
                label.col-sm-2.control-label Summary
                .col-sm-10
                    input.form-control(ng-model="event.eventSummary" placeholder="note summary")

    .row.pull-right
        .col-xs-12
            a.btn.btn-default Cancel
            | &nbsp;
            a.btn.btn-primary Save
